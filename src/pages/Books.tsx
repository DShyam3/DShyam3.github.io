import { CollectionPage } from '@/collections/components/CollectionPage';
import { booksCollection } from '@/collections/books';

const Books = () => <CollectionPage config={booksCollection} />;

export default Books;
