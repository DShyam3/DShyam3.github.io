import { CollectionPage } from '@/collections/components/CollectionPage';
import { thoughtsCollection } from '@/collections/thoughts';

const Thoughts = () => <CollectionPage config={thoughtsCollection} />;

export default Thoughts;
