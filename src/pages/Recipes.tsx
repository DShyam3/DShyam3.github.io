import { CollectionPage } from '@/collections/components/CollectionPage';
import { recipesCollection } from '@/collections/recipes';

const Recipes = () => <CollectionPage config={recipesCollection} />;

export default Recipes;
