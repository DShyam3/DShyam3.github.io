import { CollectionPage } from '@/collections/components/CollectionPage';
import { inventoryCollection } from '@/collections/inventory';

const Inventory = () => <CollectionPage config={inventoryCollection} />;

export default Inventory;
