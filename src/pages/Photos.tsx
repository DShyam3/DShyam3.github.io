import { CollectionPage } from '@/collections/components/CollectionPage';
import { photosCollection } from '@/collections/photos';

const Photos = () => <CollectionPage config={photosCollection} />;

export default Photos;
