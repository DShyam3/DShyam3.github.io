# Local development stack configuration.
#
# `project_id` is the only line the CLI strictly needs for `supabase link`, but
# leaving it at that means `supabase start` comes up with defaults that do not
# resemble this project -- wrong Postgres major, signup enabled, no site URL.
# What follows mirrors production closely enough that a local stack is worth
# starting.
#
# Note the split: this file configures the LOCAL stack. The hosted project is
# configured in the dashboard. Where the two disagree, the dashboard wins for
# production -- see SECURITY.md for the settings that are still only set there.

project_id = "yvtiybyuifkiwyrnjebe"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
# The site reads whole tables (a few hundred rows at most). This cap exists so
# an unbounded query cannot become an unbounded response.
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
# Production runs Postgres 15 (supabase-postgres-15.8.x). Matching it locally is
# the point of pinning this -- a 17 shadow database will happily accept SQL that
# production rejects.
major_version = 15

[db.pooler]
enabled = false

# Applied by `supabase db reset`, after migrations. Creates the administrator
# account and the synthetic finance profile that automated smoke tests sign in
# to; it refuses to run against a database that already holds any account.
[db.seed]
enabled = true
sql_paths = ["./seed.sql"]

[studio]
enabled = true
port = 54323

# Catches outbound mail locally instead of sending it. Named [inbucket] until
# the CLI renamed the section; the old name still works but warns on every
# command.
[local_smtp]
enabled = true
port = 54324

[storage]
enabled = true
# The `photos` and `documents` buckets both cap at 50 MB in production.
file_size_limit = "50MiB"

[auth]
enabled = true
site_url = "http://localhost:8080"
additional_redirect_urls = ["https://dshyam3.github.io"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
# Single-account site: every RLS policy checks one admin email, so a
# self-registered account gets nothing. Signup is off here; it is still ON in
# the hosted project and should be turned off in the dashboard (S-6).
enable_signup = false

[auth.email]
# Turns the email provider on. The CLI maps this key to
# GOTRUE_EXTERNAL_EMAIL_ENABLED, which gates sign-IN as well as sign-up, so
# setting it false disables the site's password box entirely -- the local stack
# rejected every attempt with `email_provider_disabled`, which reads like a bad
# password rather than a missing setting.
#
# Registration is still closed: `enable_signup = false` under [auth] above sets
# GOTRUE_DISABLE_SIGNUP, which is the global switch. The two keys share a name
# and do different jobs.
enable_signup = true
double_confirm_changes = true
enable_confirmations = false

[analytics]
enabled = false
