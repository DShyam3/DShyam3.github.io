# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
# supabase/config.toml is committed: it holds only project_id (no secrets)
# and a fresh clone needs it to run any CLI command.
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Graphify output
graphify-out/
.graphify_version

# AI/Agent metadata
.gemini/
.ai/
.cursor/

# Build and Temp
dist/
dist-ssr/
coverage/
.cache/
.turbo/
.vercel/
.netlify/

# Python bytecode cache (scripts/*.py font-build tooling)
__pycache__/
*.pyc
# Launch-readiness scan reports (should-i-ship). Regenerated per run, and they
# name specific files and weaknesses -- not something to publish in a repo.
.should-i-ship/
